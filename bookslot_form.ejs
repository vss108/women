<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Book Slot - <%= lab.name %></title>
  <style>
    body{font-family:Arial, sans-serif;background:#f9f9f9;margin:0}
    main{max-width:700px;margin:32px auto;padding:20px}
    .card{background:#fff;padding:20px;border-radius:8px;box-shadow:0 6px 18px rgba(0,0,0,0.06)}
    label{display:block;margin-top:12px;font-weight:600}
    input,select,textarea{width:100%;padding:8px;margin-top:6px;border-radius:6px;border:1px solid #ddd}
    button{margin-top:14px;padding:10px 14px;border-radius:6px;background:#28a745;color:#fff;border:none}
  </style>
</head>
<body>
<main>
  <div class="card">
    <h2>Book Slot: <%= lab.name %></h2>
    <p><strong>Location:</strong> <%= lab.location %></p>
    <p><strong>Contact:</strong> <%= lab.phone || 'N/A' %></p>

    <form method="post" action="/book-slot">
      <input type="hidden" name="labId" value="<%= lab._id %>">

      <% if (errors && errors.length) { %>
        <div style="background:#ffe6e6;padding:10px;border-radius:6px;margin-bottom:8px">
          <strong>Fix the following:</strong>
          <ul>
            <% errors.forEach(function(e){ %>
              <li><%= e.msg %></li>
            <% }) %>
          </ul>
        </div>
      <% } %>

      <label for="name">Your Name</label>
      <input id="name" name="name" required value="<%= (form && form.name) ? form.name : ((user && user.name) ? user.name : '') %>">

      <label for="phone">Phone</label>
      <input id="phone" name="phone" value="<%= (form && form.phone) ? form.phone : ((user && user.phone) ? user.phone : '') %>">

      <label for="email">Email</label>
      <input id="email" name="email" type="email" value="<%= (form && form.email) ? form.email : ((user && user.email) ? user.email : '') %>">

      <label for="date">Preferred Date</label>
      <input id="date" name="date" type="date" required value="<%= (form && form.date) ? form.date : '' %>">

      <label for="time">Preferred Time</label>
      <input id="time" name="time" type="time" required value="<%= (form && form.time) ? form.time : '' %>">

      <label for="notes">Notes / Tests required</label>
      <textarea id="notes" name="notes" rows="3"><%= (form && form.notes) ? form.notes : '' %></textarea>

      <button type="submit">Book Slot</button>
    </form>
  </div>
</main>
</body>
</html>
