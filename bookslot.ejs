<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Pregnancy Care Labs</title>

<style>
  /* Background Animation */
  body {
    font-family: Arial, sans-serif;
    margin: 0;
    background: linear-gradient(135deg, #ffe6e6, #fff0f5, #ffe6ff);
    background-size: 400% 400%;
    animation: bgAnim 18s infinite alternate;
  }
  @keyframes bgAnim {
    0% {background-position: 0% 0%;}
    50% {background-position: 100% 50%;}
    100% {background-position: 50% 100%;}
  }

  /* Header Styling */
  header {
    background: #ff5b8a;
    padding: 14px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: 0 4px 20px rgba(255, 0, 102, 0.2);
  }
  .logo {
    font-size: 22px;
    font-weight: 800;
    color: #fff;
    text-shadow: 0 2px 4px rgba(0,0,0,0.2);
  }
  .top-right a {
    color: #fff;
    margin-left: 10px;
    padding: 8px 12px;
    border-radius: 20px;
    font-size: 14px;
    text-decoration: none;
    transition: 0.3s;
    background: #0077cc;
  }
  .top-right a:hover { background: #005fa3; }
  .logout { background: #d80032 !important; }

  main {
    max-width: 1200px;
    margin: 40px auto;
    padding: 0 20px;
  }
  h1 {
    text-align: center;
    color: #e6004c;
    font-size: 32px;
    letter-spacing: 1px;
    margin-bottom: 25px;
    animation: fadeInDown 1s ease-out;
  }

  /* Fade Animation */
  @keyframes fadeInDown {
    from {opacity:0; transform: translateY(-20px);}
    to {opacity:1; transform: translateY(0);}
  }

  /* Cards Layout */
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit,minmax(290px,1fr));
    gap: 22px;
  }
  .card {
    background: #ffffff;
    padding: 20px;
    border-radius: 16px;
    box-shadow: 0 6px 20px rgba(0,0,0,0.10);
    position: relative;
    transition: transform 0.4s, box-shadow 0.4s;
    animation: fadeIn 1.2s ease-in-out;
  }
  @keyframes fadeIn {
    from {opacity:0; transform: scale(0.8);}
    to {opacity:1; transform: scale(1);}
  }
  .card:hover {
    transform: translateY(-8px);
    box-shadow: 0 10px 30px rgba(255,0,85,0.25);
  }
  .card h3 {
    margin: 0 0 6px;
    color: #c70039;
    font-size: 20px;
  }
  .meta {
    color: #333;
    font-size: 14px;
    margin: 6px 0;
  }
  .rating {
    color: #ffc400;
    font-size: 18px;
    margin: 10px 0;
    letter-spacing: 2px;
  }
  .reviews {
    font-size: 13px;
    color: #444;
    max-height: 80px;
    overflow: auto;
    margin-bottom: 12px;
    line-height: 1.4;
  }

  /* Buttons */
  .actions {
    margin-top: 12px;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 8px;
  }
  .actions a {
    text-decoration: none;
    padding: 10px 12px;
    border-radius: 25px;
    font-size: 14px;
    color: #fff;
    font-weight: 500;
    transition: all 0.3s;
    min-width: 90px;
    text-align: center;
  }
  .book { background: #28a745; }
  .call { background: #007bff; }
  .chart { background: #ff9800; }
  .actions a:hover {
    transform: scale(1.08);
    opacity: 0.85;
  }

  @media (max-width:700px){
    h1{font-size:24px;}
    .grid{grid-template-columns:1fr;}
  }

</style>
</head>

<body>
<header>
  <div class="logo">Pregnancy Care</div>
  <div class="top-right">
    <% if (name) { %>
      <span style="color:#fff;margin-right:8px">Welcome, <%= name %></span>
      <a href="/stage1">Start Personal Form</a>
      <a href="/precautions">Precautions</a>
      <a href="/logout" class="logout">Logout</a>
    <% } %>
  </div>
</header>

<main>
  <h1>Nearby Diagnostic Labs</h1>

  <div class="grid">
    <% if(labs && labs.length){ %>
      <% labs.forEach(lab => { %>
        <div class="card">
          <h3><%= lab.name %></h3>
          <div class="meta"><strong>Address:</strong> <%= lab.address %></div>
          <div class="meta"><strong>Location:</strong> <%= lab.location %></div>
          <div class="meta"><strong>Contact:</strong> <%= lab.phone %></div>

          <div class="rating">
            <% for(let i=1;i<=5;i++){ %>
              <% if(i <= lab.rating){ %>★<% } else { %>☆<% } %>
            <% } %>
          </div>

          <div class="reviews">
            <% lab.reviews.slice(0,3).forEach(r => { %>
              • <%= r %><br>
            <% }) %>
          </div>

          <div class="actions">
            <a class="book" href="/book-slot/<%= lab._id %>">Book Slot</a>
            <a class="call" href="tel:<%= lab.phone %>">Call</a>
            <a class="chart" href="/chart/<%= lab._id %>">View Chart</a>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p style="text-align:center">No labs found</p>
    <% } %>
  </div>
</main>

</body>
</html>
